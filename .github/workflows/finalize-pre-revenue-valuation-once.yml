name: Finalize canonical pre-revenue valuation once

on:
  push:
    branches:
      - fix/compare-pre-revenue-ssot-hardening

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/compare-pre-revenue-ssot-hardening
          fetch-depth: 0

      - name: Apply canonical valuation wiring
        run: python scripts/temporary/finalizeCanonicalPreRevenueValuation.py

      - name: Install
        run: npm install

      - name: Regression build
        run: npm run build

      - name: Commit canonical valuation wiring
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet; then
            echo "No source changes"
            exit 0
          fi
          git add src/lib/corporate/snapshot/types.ts src/lib/snapshot/runCorporateSnapshot.ts src/lib/corporate/preRevenueMetrics.ts src/lib/corporate/__tests__/preRevenueMetrics.test.ts package.json
          git commit -m "Canonicalize Compare pre-revenue valuation outputs"
          git push origin HEAD:fix/compare-pre-revenue-ssot-hardening
